import 'package:flutter/material.dart';

class PacCadPage extends StatelessWidget {
  const PacCadPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Nenhum paciente foi cadastrado.'),
    );
  }
}
